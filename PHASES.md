# Фазы реализации

Документ описывает этапы разработки приложения.

---

## Фаза 1: Основа ✅ ЗАВЕРШЕНА

- [x] Настройка проекта Symfony 7 с Docker
- [x] Конфигурация API Platform со Swagger UI
- [x] Схема базы данных и Doctrine сущности (включая роли пользователей)
- [x] Настройка JWT аутентификации
- [x] Ограничения валидации на бэкенде
- [x] Базовый CRUD для заметок с пагинацией

## Фаза 2: Основные функции заметок ✅ ЗАВЕРШЕНА

- [x] Проект Vue 3 с Vite, TypeScript, Tailwind CSS
- [x] Mobile-first адаптивная структура лейаута
- [x] Pinia stores для авторизации и заметок
- [x] Валидация на фронтенде с VeeValidate + Zod
- [x] Интеграция редактора Milkdown
- [x] Живой предпросмотр
- [x] Автосохранение с уведомлением о статусе

## Фаза 3: Организация ✅ ЗАВЕРШЕНА

- [x] Сущность папки и API
- [x] Компонент дерева папок в боковой панели (адаптивный)
- [x] Drag-and-drop для заметок и папок *(перетаскивание папок снято в фазе 5; DnD заметок между папками — фаза 17)*
- [x] Реализация тегов
- [x] Endpoint поиска заметок и UI
- [x] Интеграция компонентов в существующие views
- [x] Адаптивный drawer для sidebar
- [x] Панель метаданных заметки

## Фаза 4: Расширенные функции ✅ ЗАВЕРШЕНА

- [x] UI корзины
- [x] Запланированная команда очистки корзины
- [x] API endpoints администрирования пользователей (статистика)
- [x] UI панели администратора (список пользователей, активация/деактивация/удаление)
- [x] Сервис парсинга wiki-ссылок
- [x] Рендеринг wiki-ссылок в предпросмотре
- [x] Панель обратных ссылок
- [x] История версий с просмотром различий

## Фаза 5: Доработки вёрстки ✅ ЗАВЕРШЕНА

### Структура и навигация

- [x] Унифицировать `AppLayout` на всех экранах (корзина, теги, админка сейчас без navbar/sidebar)
- [x] Поиск на мобильных: иконка в navbar → modal/drawer (сейчас `SearchBar` скрыт до `md`)

### Sidebar / дерево папок

- [x] Убрать иконку перетаскивания у папок (папки сортируются по алфавиту, drag-and-drop не нужен)
- [x] Добавить иконку "+" рядом с папками при наведении для быстрого создания подпапки
- [x] Исправить hover-действия папок (кнопки редактирования/удаления не появляются — нет класса `group` на родителе)
- [x] Унифицировать отступы и иконки в дереве папок (chevron, folder, actions на одной линии)
- [x] Визуальная иерархия вложенных папок (направляющие линии, открытая/закрытая иконка)
- [x] Фильтрация заметок на dashboard по выбранной папке

### Dashboard и метаданные заметок

- [x] На карточках заметок в общем списке (`DashboardView`) показывать название папки, в которой находится заметка (с урезанием имени, чтобы помещалось в карточку)
- [x] При создании заметки при выбранной папке в сайдбаре — предзаполнять эту папку в метаданных (`NoteMetadata` / `FolderSelector`)

### Баги

- [x] Переименование заметки создаёт новую заметку вместо обновления существующей
- [x] Не отображаются версии заметки

### Редактор

- [x] Выделение активного форматирования в тулбаре редактора
- [x] Стабильность курсора при автосохранении (позиция не должна изменяться)
- [x] Общие адаптивные стили markdown для editor и preview (единый класс `.markdown-prose` в `main.css`)
- [x] Адаптивный размер поля заголовка заметки (`text-xl` на средних экранах, `text-2xl` на больших)
- [x] Touch targets в тулбаре редактора (минимум ~44px на мобильных)

### Адаптив

- [x] Единая сетка breakpoints: 768 / 1024 / 1400 (`useBreakpoints`, tailwind `md`/`lg`/`3xl`)
- [x] Сайдбар с папками и тегами (`AppSidebar`): при ширине экрана < 1024px сворачивается влево с возможностью открыть
- [x] Сайдбар с метаданными (`NoteMetadata`): при ширине >= 1400px — фиксированная панель справа; при < 1400px — drawer по кнопке в toolbar `NoteView`
- [x] Перекомпоновка адаптивного layout: `AppSidePanel`, кнопка навигации в navbar, убраны floating-кнопки
- [x] Уменьшить ширину боковых панелей на средних экранах (`w-64 lg:w-72 3xl:w-80`)
- [x] Доработать мобильную версию NoteView (компактная панель, overflow-меню для второстепенных действий)
- [x] Убрать лишний верхний отступ на мобильных (удалён двойной `pt-16`; toggle-кнопки перенесены в navbar и toolbar заметки)

### Типографика и масштаб

- [x] Адаптивные размеры шрифтов и отступов: сейчас типографика и spacing выглядят корректно только при ширине окна > 1400px; для меньших экранов пропорционально уменьшить заголовки, основной текст, padding и margin

### Консистентность

- [x] **Единая система отступов** — зафиксировать шкалу в `main.css` и применить во всех layout-компонентах:
  - расширить существующие классы (`.page-container`, `.panel-padding`, `.stack-sections` и др.);
  - добавить `.stack-items`, `.list-row-padding` для списков и строк дерева;
  - заменить ad-hoc `p-*` / `mb-*` в `AppSidePanel`, sidebar, views (dashboard, корзина, теги, админка);
  - не трогать spacing в `.markdown-prose`
- [x] **Палитра `surface-*` для хрома UI** — унифицировать смешение `gray-*` и `surface-*` (navbar, sidebar, main, карточки):
  - semantic-алиасы в `main.css`: `.app-chrome`, `.app-ground`, `.app-border`, `.text-muted`;
  - миграция layout/views на `surface-*`; `gray-*` оставить в markdown и legacy `.btn-*`
- [x] **Адаптивные модальные окна** — единый паттерн `90vw` + `maxWidth`:
  - константы размеров в `frontend/src/constants/modal.ts` (`sm` / `md` / `lg` / `xl`);
  - применить ко всем `Dialog` (`SearchBar`, папки, теги, корзина, diff версий, mobile search);
  - исправить `SearchBar` (сейчас фиксированные `50rem`)
- [x] **Локализация истории версий** — перевести на русский и унифицировать даты:
  - `VersionHistoryPanel`, `RestoreVersionModal`, `VersionDiff`;
  - вынести `formatRelativeDate` в `utils/date.ts` (формат как на dashboard, `ru-RU`);
  - опционально: `RestoreVersionModal` → PrimeVue `Dialog` + `MODAL_WIDTH.md`
- [x] **Actions на карточках dashboard (touch)** — заметные действия без hover:
  - `@media (hover: hover)` — приглушение actions, полная видимость при hover карточки;
  - `@media (hover: none)` — actions всегда видимы;
  - touch targets ≥ 44px (размер кнопок или overflow-меню на мобильных)
- [x] **Пустые состояния** — общий компонент `EmptyState.vue` (icon → title → description → slot actions):
  - dashboard (нет заметок / пустая папка);
  - корзина;
  - поиск без результатов (full results в `SearchBar`)

## Фаза 6: Доработки по режимам редактирования и автосохранению ✅ ЗАВЕРШЕНА

- [x] Исправить режимы редактирования заметок (убрать split режим, оставить только просмотр/редактирование)
- [x] Исправить отображение текста в режиме редактирования (добавлена панель форматирования, исправлено обновление контента)
- [x] Улучшить логику автосохранения версий (`NoteVersionService`):
  - если с момента последнего `updatedAt` заметки прошло меньше N минут — **не создавать** новую версию (версии неизменяемы, обновляется только заметка);
  - если содержимое (title/content) совпадает с предыдущей версией — **не создавать** версию (например, пробел и затем backspace);
  - после истечения N минут с последнего `updatedAt` заметки — создавать новую версию со снимком состояния **до** текущего сохранения
- [x] Вынести в параметры интервалы автосохранения и версионирования:
  - **бэкенд:** поля `autosave_delay_seconds`, `version_consolidation_window_minutes` в `users`; системные defaults — env (`AUTOSAVE_DELAY_SECONDS`, `NOTE_VERSION_CONSOLIDATION_WINDOW_MINUTES`); `UserSettingsResolver` + `PATCH /api/auth/settings`;
  - **фронтенд:** `SettingsView` (`/settings`), селекты (автосохранение: 5–60 сек; версионирование: 1–60 мин); пустое значение → NULL (системный default); переход по клику на email в navbar

## Фаза 7: Избранные заметки ✅ ЗАВЕРШЕНА

### Бэкенд

- [x] Поле `is_favorite` (boolean, default `false`) в сущности `Note`; обновлена существующая миграция `Version20260609085911` (отдельная не создаётся — приложение не деплоилось)
- [x] Поле в группах сериализации `note:read` / `note:write`; обновление через `PUT /api/notes/{id}`
- [x] Фильтр API Platform по `isFavorite` (и при необходимости по `folder.id`) для выборки избранных
- [x] Сортировка избранных по `updatedAt` (desc) — `order` на `ApiResource` + `OrderFilter`

### Фронтенд

- [x] Тип `Note` и API-клиент: поле `isFavorite`, метод переключения статуса избранного
- [x] Иконка со звёздочкой на карточке заметки в списке (`DashboardView`): клик переключает статус без перехода в заметку
- [x] Блок «Избранные» перед общим списком заметок:
  - отображается **только на первой странице** пагинации;
  - избранные сортируются по дате обновления (новые сверху);
  - в общем списке (все заметки) — все избранные пользователя;
  - при выбранной папке в сайдбаре — только избранные из этой папки
- [x] Избранные **исключаются** из общего пагинируемого списка (`isFavorite=false`); отображаются только в блоке «Избранные»
- [x] Обновление store после переключения звёздочки (локально + повторная загрузка при смене страницы/папки)

## Фаза 8: Доработка фильтрации тегов ✅ ЗАВЕРШЕНА

### Бэкенд

- [x] Поиск/список заметок с фильтром по тегам — логика **И** (заметка должна содержать **все** выбранные теги; сейчас в `NoteRepository::search` — `IN`, т.е. ИЛИ)
- [x] Поддержка комбинированного фильтра: `folderId` + `tags[]` (для dashboard при выбранной папке и тегах)
- [x] При необходимости: endpoint или параметры для получения тегов, встречающихся в заметках папки / в результате текущей выборки

### Фронтенд

- [x] Фильтрация заметок на `DashboardView` по выбранным тегам в сайдбаре (`TagsPanel` → сейчас `TODO` в `AppLayout.handleTagFilter`)
- [x] Множественный выбор тегов с логикой **И** (передача всех `selectedTags` в API)
- [x] При выборе папки в сайдбаре — в списке тегов показывать **только теги заметок этой папки** (не все теги пользователя)
- [x] После выбора тега(ов) и фильтрации заметок — **сузить список тегов** в сайдбаре: остаются только теги, присутствующие в текущем отфильтрованном списке заметок
- [x] Сброс/согласование фильтров: смена папки, «Очистить фильтры», пагинация; заголовок dashboard отражает активные фильтры
- [x] Сохранение выбранных тегов в store (`tagsStore.selectedTags`) синхронизировано с загрузкой заметок

## Фаза 9: Переработка интерфейса ✅ ЗАВЕРШЕНА

### Тулбар и навигация

- [x] Приведение положений кнопок к единому виду (тулбар заметки: избранное слева от заголовка; действия справа; одинаковый набор кнопок на mobile/desktop; скрытие кнопок при редактировании заголовка)

#### Footer левого сайдбара (системная навигация)

- [x] Перенос корзины в footer sidebar (над блоком аккаунта); badge с количеством удалённых заметок
- [x] Перенос «Управление пользователями» в footer sidebar отдельной строкой (только `ROLE_ADMIN`, над блоком аккаунта)
- [x] Перенос аккаунта в footer sidebar: email/настройки → `/settings`, кнопка «Выход»
- [x] Убрать из navbar: корзину, админку, email, настройки, выход (navbar: лого, toggle навигации, поиск, «Новая заметка»)

#### Кнопка «Новая заметка»

- [x] Обсудить и зафиксировать положение кнопок добавления заметок и папок:
  - заметка — **app header (navbar)**, скрывать на корзине;
  - папка — `+` в header секции «Папки»; подпапка — hover на строке папки
- [x] Оставить в **app header (navbar)** — доступна с любой страницы
- [x] Скрывать на странице корзины (`route.name === 'trash'`) и guest-страницах
- [x] Создание папки — без изменений: `+` в header секции «Папки»; подпапка — hover на строке папки

#### Контекст при создании заметки

- [x] Вынести логику в общий composable (например `useCreateNote`), использовать в navbar и `DashboardView` / `EmptyState`
- [x] **Черновик без сохранения:** «Новая заметка» открывает `/notes/new`; POST только после появления текста в теле заметки
- [x] **На странице заметки** (`NoteView`): новая заметка наследует **папку и теги** из актуальных метаданных редактора (`syncActiveNoteContext`)
- [x] **На остальных страницах**: папка — `foldersStore.selectedFolderId`; теги — выбранные в фильтре сайдбара (`tagsStore.selectedTags`)
- [x] Frontend: расширить `CreateNoteRequest` и `notesApi.create` поддержкой `tags` (API уже принимает `tags` в `note:write`)

## Фаза 10: Оптимизация списков заметок ✅ ЗАВЕРШЕНА

> Подготовка (выполнено): описание стейтов на фронтенде — раздел «Управление состоянием на фронтенде» в `ARCHITECTURE.md`

- [x] **Список заметок без полного `content` в store:**
  - **бэкенд:** группа сериализации `note:list` для collection (`GET /notes`, `/notes/trash`, `/notes/search`); виртуальное поле `contentPreview` через `NotePreviewService` (150 символов);
  - **фронтенд:** тип `NoteListItem` vs `Note`; `notes` / `favoriteNotes` в Pinia — только list-DTO; полный `content` — в `currentNote` и локальных refs редактора;
  - обновлены `NoteCard`, API-клиент, поиск/фильтрацию, корзина; превью — из `contentPreview` с бэкенда; `toNoteListItem()` для синхронизации списков после мутаций

## Фаза 11: Настройки аккаунта ✅ ЗАВЕРШЕНА

Изолированные изменения в `/settings` и глобальная тема — до массовых доработок UX, чтобы новые компоненты сразу стилизовались под обе темы.

- [x] Смена пароля пользователем (backend: `POST /api/auth/change-password` с проверкой текущего пароля; frontend: collapsible-форма в карточке «Аккаунт» на `/settings`)
- [x] Переключение тёмной / светлой темы:
  - [x] сохранение выбора в `localStorage` (без поля в `users`);
  - [x] инициализация по `prefers-color-scheme` при первом визите;
  - [x] semantic-токены / `dark:` в Tailwind, PrimeVue dark mode (`useTheme`, `lara-dark-blue`, переключатель в `/settings`)

## Фаза 12: UX редактора и приложения ✅ ЗАВЕРШЕНА

Поведение и cross-cutting UX на уже зафиксированных list-DTO и теме.

- [x] Автозаголовок при создании заметки: если заголовок не менялся с дефолтного («Новая заметка»), после ввода первого абзаца в редакторе сформировать заголовок из первого предложения; при длине > 128 символов — обрезать по границе слова; фокус и позиция курсора в редакторе не должны сбрасываться
- [x] Горячие клавиши (навигация, действия, форматирование в тулбаре); при необходимости — подсказка/шпаргалка в UI

### Баги

- [x] Wiki-ссылки по UUID: `[[uuid]]` / `[[uuid|отображаемый текст]]`; вставка только через модалку (кнопка, `Ctrl+Alt+W`, ввод `[[`); разрешение и переход в preview по ID; legacy title-ссылки удалены

- [x] **Динамический индикатор сохранения** (`SaveIndicator` / `useAutosave`):
  - «Сохранение...» во время запроса;
  - после успеха — «Сохранено N секунд назад» / «N минут назад» с периодическим обновлением текста (не статичное «Сохранено»);
  - при ошибке — текущее поведение; `formatSavedAgo` в `utils/date.ts` (секунды / минуты; для часов — `formatRelativeDate`)
- [x] Состояния загрузки и обработка ошибок:
  - единые паттерны loading / empty / error в views и stores;
  - toast или inline-сообщения для сбоев API;
  - не дублировать ad-hoc `isLoading` / `alert` в новых местах
- [x] **Подгрузка заметок вместо пагинации** (`DashboardView`, при необходимости поиск и избранные):
  - убрать постраничную навигацию (кнопки / номера страниц);
  - при прокрутке списка (или sentinel внизу) — запрос следующей страницы API и **добавление** в store (`append`, не замена);
  - индикатор загрузки следующей порции; блокировка дублирующих запросов;
  - сброс списка при смене папки, тегов, поиска
- [x] **Избранные — отдельный вид** (вместо блока над списком на dashboard):
  - убрать блок «Избранные» с `DashboardView`; списки dashboard и папок включают избранные (со звёздочкой);
  - маршрут `/favorites` и пункт навигации в sidebar (перед папками);
  - `FavoritesView`: полный список избранных; в dashboard и папках избранные тоже видны (со звёздочкой)
  - переиспользовать `notesStore.fetchFavorites`, `NoteCard`, переключение звёздочки; подгрузка — по тому же паттерну, что и на dashboard

## Фаза 13: Экспорт заметок ✅ ЗАВЕРШЕНА

Additive-фича; опирается на установленные loading/error-паттерны и тему. **Демо MVP:** только экспорт **одной** заметки из `NoteView` (массовый экспорт и выбор набора — вне scope). **Без backend-endpoint** и **без jsPDF/html2canvas**.

- [x] Кнопка «Экспорт» в тулбаре `NoteView` → меню «Markdown» / «PDF»

### Markdown (клиент)

- [x] Скачивание `.md` через `Blob` + programmatic download
- [x] **Markdown:** заголовок — `# …` в теле; метаданные — `### Метаданные` + список в футере; даты human-readable (`ru-RU`)
- [x] **wiki-ссылки в теле:** `[\[\[label\]\]]` (alias или title); `sanitizeNoteText` убирает nbsp и прочие нечитаемые символы
- [x] Имя файла: санитизированный `title`, fallback `note-{shortId}.md`
- [x] Утилита `buildMarkdownExport()` + `getFolderPath()` (полный путь по дереву `foldersStore`)

### Баги (отложено)

- [x] Нечитаемые символы Unicode (U+00A0 nbsp, zero-width и др.) не должны сохраняться в `title`/`content` заметки — нормализация при вводе и автосохранении (фронт) и при `POST`/`PUT` (бэкенд)

### PDF (клиент, `window.print`)

- [x] Маршрут `/notes/:id/print` — без navbar/sidebar (`NotePrintView`)
- [x] Страница `NotePrintView`: метаданные `NoteExportMetadata` + `MarkdownPreview` с телом заметки
- [x] Светлая тема на print-странице; `@media print` в `main.css`
- [x] `?auto=1` — `window.print()` после готовности preview
- [x] `document.title` = заголовок заметки (дефолтное имя PDF в диалоге печати)

## Фаза 14: Граф связей

Локальная визуализация wiki-связей (`NoteLink`, `WikiLinkParser`). **Без отдельной страницы** — кнопка в `NoteMetadata` вместо `BacklinksPanel`. Битые ссылки (UUID без target) **не** попадают в граф.

### 14.1 Alias wiki-ссылок в `note_links` (prerequisite) ✅ ЗАВЕРШЕНА

- [x] колонка `aliases JSONB NOT NULL` в `note_links`; одна строка на пару `(source_note_id, target_note_id)`;
  - элемент массива — alias из `[[uuid|alias]]` или `null` для `[[uuid]]` (при отображении подставлять актуальный `target.title`);
  - порядок элементов — порядок появления ссылок на этот target в тексте source-заметки; длина массива = число вхождений;
- [x] синхронизация при POST/PUT/PATCH: `WikiLinkParser::parseLinksWithAliases()` → группировка по target → upsert / удаление устаревших строк (`NoteLinkSyncService`);
- [x] миграция `Version20260613120000`; alias заполняются при следующем POST/PUT/PATCH заметки (или после сброса БД);
- global order ссылок в заметке и поиск по alias — вне scope

### 14.2 API графа и linkStats ✅ ЗАВЕРШЕНА

- [x] `GET /api/notes/{id}/graph?depth=N` — локальный subgraph (BFS от заметки):
  - `depth`: 1–3, default **1**; опционально «+1 уровень» — повторный запрос с `depth+1`, merge на клиенте;
  - `maxNodes` (~100–150) на бэкенде; при обрезке — `truncated: true` и `frontierNodeIds[]`;
  - ответ: `nodes` (id, title, folderId?, isFavorite?) + `edges` (id, source, target, aliases[]);
  - подпись ребра: первый не-null alias или `target.title`; при `aliases.length > 1` — «alias ×N», полный список в tooltip;
  - query `direction`: `both` (default) | `outgoing` | `incoming` — опционально для MVP;
  - только связи из `note_links` (битые исключены)
- [x] `linkStats` в `note:read`: `{ incoming: number, outgoing: number }` — для видимости кнопки без загрузки графа
- [x] `versionCount` в `note:read` — для блока «Информация» в метаданных

### 14.3 UI: граф вместо обратных ссылок ✅ ЗАВЕРШЕНА

- [x] библиотека: **vis-network** (`^10.1.0`); force-directed, directed edges, zoom/pan/drag;
- [x] `NoteLinksGraphDialog` (Dialog, `MODAL_WIDTH.xl`, fullscreen `< md`): граф + кнопка «+1 уровень» при `truncated` / frontier;
- [x] клик по узлу → `/notes/:id?mode=preview`; текущая заметка — подсветка (focus);
- [x] кнопки «Связанные заметки» (`pi-share-alt`) и «История версий» (`pi-history`) в тулбаре `NoteView` → модалки; граф — **только если** `incoming > 0 OR outgoing > 0`;
- [x] `VersionHistoryDialog`; сайдбар метаданных — папка, теги, информация (`versionCount`);
- [x] loading / error / empty — по паттернам фазы 12; endpoint `/notes/{id}/backlinks` можно оставить в API, из UI убрать

### 15 Demo seed (консольная команда) ✅ ЗАВЕРШЕНА

Команда для наполнения БД базовым набором данных — dev/demo и ручная проверка графа связей. **Объём фикстуры большой** — осознанно: для демо и скринкаста должно выглядеть «живой» базой знаний.

- [x] консольная команда `app:seed-demo-data`; флаг `--force` — удалить существующих demo-пользователей и пересоздать данные;
- [x] **3 пользователя** (email + пароль `demo1234`; все `ROLE_USER`); **контент каждого — отдельная вселенная**, без пересечений между аккаунтами:
  - **Пользователь 1 — вселенная Гарри Поттера** (персонажи, факультеты, заклинания, артефакты, события книг);
  - **Пользователь 2 — мир Игры престолов** (дома, персонажи, локации, битвы, интриги);
  - **Пользователь 3 — мир Ведьмака** (ведьмаки, чудовища, локации, квесты, алхимия/знаки);
- [x] у каждого пользователя **5–10 папок** по темам вселенной (иерархия до 3 уровней); часть заметок в корне без папки;
- [x] у каждого **30–50 заметок** с разнообразным markdown (заголовки, списки, цитаты, таблицы где уместно);
- [x] **wiki-ссылки** только между заметками **того же** пользователя; после создания — `NoteLinkSyncService`;
- [x] **теги** — словарь 8–12 тематических тегов на вселенную; 1–3 тега на заметку; пересечения для проверки фильтра «И» в sidebar;
- [x] **история версий** — 3–5 заметок на вселенную с 2–4 снимками; такие заметки помечать **избранными** (`isFavorite`);
- [x] данные вынесены в `DemoSeed/Universe/*Universe.php` (Potter, Westeros, Witcher);
- [x] документировано в README / `ARCHITECTURE.md` (запуск через `docker compose exec php …`, учётные данные demo-пользователей); администратор — отдельно `app:create-admin`

> **Спецификация реализации** — см. [`demoseed.md`](./demoseed.md).

## Фаза 16: Иконки папок ✅ ЗАВЕРШЕНА

Задача была добавлена в промпте 82 (изначально фаза 9 «Переработка интерфейса»), случайно удалена при закрытии фазы 9 в коммите `f319eac` («Phase 9.2 Context update»); в коде не реализована.

- [x] **Бэкенд:** поле `icon` в сущности `Folder` (миграция `Version20260613130000`, группы `folder:read` / `folder:write`, валидация через `FolderIconChoices` + `Assert\Choice`)
- [x] **Фронтенд:** `FolderIconPicker` в диалогах создания/редактирования папки; отображение иконки в дереве сайдбара (`resolveFolderTreeIcon`; fallback по уровню 0/1/2: `pi-briefcase` / `pi-folder` / `pi-book`)

## Фаза 17: Drag-and-drop замetок между папками ✅ ЗАВЕРШЕНА

Перетаскивание заметок из списка (dashboard, избранное) на папку в сайдбаре. В фазе 3 задача была отмечена выполненной, но в коде остался только зависимый пакет `vue-draggable-plus`; перетаскивание **папок** сознательно снято в фазе 5 (сортировка по имени). Смена папки через `FolderSelector` в метаданных заметки уже работает — эта фаза про **DnD из списка в дерево**.

### Бэкенд

- [x] Отдельный endpoint не обязателен: `PATCH /api/notes/{id}` с `folder` (IRI или `null`) поддерживается; smoke-проверка смены `folder_id` без изменения `title`/`content`

### Фронтенд — drag-and-drop (desktop)

- [x] **Источник:** карточка заметки (`NoteCard`) на `DashboardView`, `FavoritesView` (не поиск)
- [x] **Цель:** строки дерева папок (`FolderTree` / `FolderTreeItem`) + пункт «Все заметки» (= корень, `folder: null`)
- [x] Визуальная обратная связь: подсветка допустимой drop-зоны, индикатор при наведении; курсор/grab-handle; не мешать клику по карточке (открытие заметки)
- [x] После drop: `notesStore.moveNoteToFolder(id, folderId)`; обновить списки (убрать из текущей выборки при фильтре по папке, обновить `folder` на карточке); toast при ошибке
- [x] Только `@media (hover: hover)` / desktop — на touch без hover DnD не включать

### Фронтенд — мобильная альтернатива

- [x] Действие «Переместить в папку» в overflow/actions карточки заметки → диалог с `FolderDropdown` (тот же API, что у drag)

### Согласованность

- [x] Не дублировать логику с `FolderSelector` в `NoteView` — общий composable `useMoveNoteToFolder` + метод store `moveNoteToFolder`
- [x] Обновить `ARCHITECTURE.md` (раздел про DnD: только заметки между папками, без перетаскивания папок)

## Фаза 18: Self-review

Ревизия кодовой базы **после фаз 16–17**, перед однопользовательским режимом и финализацией.

**Ревью (аудит) выполнено** (2026-06-13). Все проверки фазы 18 разнесены по детальным чеклистам — находки, приоритеты и шаги **правок** (не путать с завершением фазы):

| Область | Документ | Шагов правок |
|---------|----------|:------------:|
| Фронтенд | [`frontend_selfreview.md`](./frontend_selfreview.md) | 14 |
| Бэкенд | [`backend_selfreview.md`](./backend_selfreview.md) | 14 |

Покрытие категорий фазы 18 в selfreview-файлах: запахи/структура, дублирование, N+1 и индексы (бэк), видимость API, согласованность паттернов, auth/IDOR, ввод-вывод/XSS, секреты/CORS/nginx, обработка ошибок.

### Исправления (фаза остаётся открытой до critical)

- [x] **Critical (фронт):** XSS в `SearchBar` (шаг 1)
- [x] **Critical (бэк):** IDOR / ownership (шаг 1)
- [ ] Остальные шаги — по приоритету в selfreview-файлах; отмечать `- [x]` по мере коммитов
- [ ] Краткая фиксация **выполненных** правок в `REPORT.md` (по ходу, не дублировать полные чеклисты)
- [ ] **Backlog после ревью** (вне шагов 1–14): см. секцию «Доработки после ревью» в `frontend_selfreview.md` / `backend_selfreview.md` (например регистронезависимый поиск)

## Фаза 19: Однопользовательский режим (без авторизации)

Развёртывание как локальной персональной базы без регистрации и входа.

- [ ] **Конфигурация:** env-флаг (например `SINGLE_USER_MODE` / `APP_AUTH_ENABLED=false`) на бэкенде и фронтенде; документировать в README / docker-compose
- [ ] **Бэкенд:** при включённом режиме — единственный пользователь (создание при первом запуске / fixtures); security firewall без JWT для API или автоматическая подстановка default user; сохранить изоляцию данных по `user_id` в коде
- [ ] **Фронтенд:** скрыть `/login`, `/register`; роутер без auth-guard; не показывать logout / email в sidebar; прямой вход на dashboard
- [ ] **Совместимость:** многопользовательский режим остаётся режимом по умолчанию; smoke-проверка обоих вариантов развёртывания

## Фаза 20: Подготовка к сдаче

Финализация на стабильной кодовой базе после фаз 10–19.

- [ ] Демо-данные для БД — см. **фаза 15** (`app:seed-demo-data`)
- [ ] **Два варианта окружения (Docker):**
  - **Разработка (текущее):** `docker-compose.yml` — сервис `node` с `npm install` и Vite dev server (`5173`); hot reload; API через nginx (`APP_PORT`, по умолчанию `8080`)
  - **Демо / продакшен:** отдельный compose-файл (например `docker-compose.prod.yml` или profile `demo`) — **без** сервиса `node`; **без** `npm install` и сборки при старте контейнеров; статический фронтенд отдаётся nginx из заранее собранного `dist` (multi-stage build образа или volume с артефактом CI); один входной URL (`APP_PORT`); документировать в README: когда какой вариант использовать, команды запуска, требование предварительной сборки фронта для demo-режима
- [ ] **Тесты — минимальный набор (smoke):**
  - **бэкенд (PHPUnit):** регистрация/логин, CRUD заметки, доступ только к своим данным; спецификации — [`future_autotests.md`](./future_autotests.md);
  - **фронтенд (Vitest):** smoke API-клиента или ключевого store (например `notes`)
- [ ] **Тесты — дополнительные (по возможности):**
  - **бэкенд:** `NoteVersionService`, `WikiLinkParser`, фильтры `NoteRepository` (теги **И**, folder + tags);
  - **фронтенд:** `useAutosave`, composables фильтрации, критичные компоненты
- [ ] Обновить `ARCHITECTURE.md` по актуальной кодовой базе (модель данных, API, структура проекта, консольные команды и прочие расхождения с реализацией)
- [ ] Удалить dev-генератор demo seed `backend/tools/build_universes.py` (контент зафиксирован в `DemoSeed/Universe/*Universe.php`; убрать упоминания из `ARCHITECTURE.md`)
- [ ] Создание скринкаста с демонстрацией использования приложения

