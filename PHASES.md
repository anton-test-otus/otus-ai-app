# Фазы реализации

Документ описывает этапы разработки приложения.

---

## Фаза 1: Основа ✅ ЗАВЕРШЕНА

- [x] Настройка проекта Symfony 7 с Docker
- [x] Конфигурация API Platform со Swagger UI
- [x] Схема базы данных и Doctrine сущности (включая роли пользователей)
- [x] Настройка JWT аутентификации
- [x] Ограничения валидации на бэкенде
- [x] Базовый CRUD для заметок с пагинацией

## Фаза 2: Основные функции заметок ✅ ЗАВЕРШЕНА

- [x] Проект Vue 3 с Vite, TypeScript, Tailwind CSS
- [x] Mobile-first адаптивная структура лейаута
- [x] Pinia stores для авторизации и заметок
- [x] Валидация на фронтенде с VeeValidate + Zod
- [x] Интеграция редактора Milkdown
- [x] Живой предпросмотр
- [x] Автосохранение с уведомлением о статусе

## Фаза 3: Организация ✅ ЗАВЕРШЕНА

- [x] Сущность папки и API
- [x] Компонент дерева папок в боковой панели (адаптивный)
- [x] Drag-and-drop для заметок и папок
- [x] Реализация тегов
- [x] Endpoint поиска заметок и UI
- [x] Интеграция компонентов в существующие views
- [x] Адаптивный drawer для sidebar
- [x] Панель метаданных заметки

## Фаза 4: Расширенные функции ✅ ЗАВЕРШЕНА

- [x] UI корзины
- [x] Запланированная команда очистки корзины
- [x] API endpoints администрирования пользователей (статистика)
- [x] UI панели администратора (список пользователей, активация/деактивация/удаление)
- [x] Сервис парсинга wiki-ссылок
- [x] Рендеринг wiki-ссылок в предпросмотре
- [x] Панель обратных ссылок
- [x] История версий с просмотром различий

## Фаза 5: Доработки вёрстки

### Структура и навигация

- [x] Унифицировать `AppLayout` на всех экранах (корзина, теги, админка сейчас без navbar/sidebar)
- [x] Поиск на мобильных: иконка в navbar → modal/drawer (сейчас `SearchBar` скрыт до `md`)

### Sidebar / дерево папок

- [x] Убрать иконку перетаскивания у папок (папки сортируются по алфавиту, drag-and-drop не нужен)
- [x] Добавить иконку "+" рядом с папками при наведении для быстрого создания подпапки
- [x] Исправить hover-действия папок (кнопки редактирования/удаления не появляются — нет класса `group` на родителе)
- [x] Унифицировать отступы и иконки в дереве папок (chevron, folder, actions на одной линии)
- [x] Визуальная иерархия вложенных папок (направляющие линии, открытая/закрытая иконка)
- [x] Фильтрация заметок на dashboard по выбранной папке

### Dashboard и метаданные заметок

- [x] На карточках заметок в общем списке (`DashboardView`) показывать название папки, в которой находится заметка (с урезанием имени, чтобы помещалось в карточку)
- [x] При создании заметки при выбранной папке в сайдбаре — предзаполнять эту папку в метаданных (`NoteMetadata` / `FolderSelector`)

### Баги

- [x] Переименование заметки создаёт новую заметку вместо обновления существующей
- [x] Не отображаются версии заметки

### Редактор

- [x] Выделение активного форматирования в тулбаре редактора
- [x] Стабильность курсора при автосохранении (позиция не должна изменяться)
- [x] Общие адаптивные стили markdown для editor и preview (единый класс `.markdown-prose` в `main.css`)
- [x] Адаптивный размер поля заголовка заметки (`text-xl` на средних экранах, `text-2xl` на больших)
- [x] Touch targets в тулбаре редактора (минимум ~44px на мобильных)

### Адаптив

- [x] Единая сетка breakpoints: 768 / 1024 / 1400 (`useBreakpoints`, tailwind `md`/`lg`/`3xl`)
- [x] Сайдбар с папками и тегами (`AppSidebar`): при ширине экрана < 1024px сворачивается влево с возможностью открыть
- [x] Сайдбар с метаданными (`NoteMetadata`): при ширине >= 1400px — фиксированная панель справа; при < 1400px — drawer по кнопке в toolbar `NoteView`
- [x] Перекомпоновка адаптивного layout: `AppSidePanel`, кнопка навигации в navbar, убраны floating-кнопки
- [x] Уменьшить ширину боковых панелей на средних экранах (`w-64 lg:w-72 3xl:w-80`)
- [x] Доработать мобильную версию NoteView (компактная панель, overflow-меню для второстепенных действий)
- [x] Убрать лишний верхний отступ на мобильных (удалён двойной `pt-16`; toggle-кнопки перенесены в navbar и toolbar заметки)

### Типографика и масштаб

- [x] Адаптивные размеры шрифтов и отступов: сейчас типографика и spacing выглядят корректно только при ширине окна > 1400px; для меньших экранов пропорционально уменьшить заголовки, основной текст, padding и margin

### Консистентность

- [ ] Единая система отступов во всех компонентах
- [ ] Унифицировать палитру `gray-*` и `surface-*` (сейчас смешаны в navbar, sidebar, main)
- [ ] Адаптивная ширина модальных окон (единый паттерн `90vw` + `maxWidth`; `SearchBar` сейчас `50rem`)
- [ ] Локализация `VersionHistoryPanel` (сейчас на английском)
- [ ] Карточки заметок на dashboard: actions видимы на touch-устройствах (не только по hover)
- [ ] Пустые состояния: dashboard, корзина, поиск без результатов

## Фаза 6: Финальная доработка

- [ ] Смена пароля пользователем (backend: endpoint с проверкой текущего пароля; frontend: форма в профиле/настройках)
- [ ] Переключение тёмной / светлой темы
- [ ] Горячие клавиши (навигация, действия, форматирование в тулбаре)
- [ ] Состояния загрузки и обработка ошибок
- [ ] Экспорт заметок (markdown, PDF)
- [ ] Seed data для базы данных (демо-пользователи, примеры заметок)
- [ ] Тесты (бэкенд: PHPUnit; фронтенд: Vitest)
- [ ] Создание скринкаста с демонстрацией использования приложения

## Доработки (выполняются в последнюю очередь)

- [x] Исправить режимы редактирования заметок (убрать split режим, оставить только просмотр/редактирование)
- [x] Исправить отображение текста в режиме редактирования (добавлена панель форматирования, исправлено обновление контента)
- [x] Улучшить логику автосохранения версий (`NoteVersionService`):
  - если с момента последней версии прошло меньше N минут — **обновлять** предыдущую версию, а не создавать новую;
  - если содержимое (title/content) совпадает с предыдущей версией — **не создавать и не обновлять** версию (например, пробел и затем backspace);
  - после истечения N минут — создавать новую версию (текущее поведение)
- [ ] Вынести в параметры интервалы автосохранения и версионирования (сейчас захардкожены):
  - **бэкенд:** окно консолидации версий (`NoteVersionService`, сейчас 5 мин) — env/конфиг Symfony;
  - **фронтенд:** debounce автосохранения (`useAutosave` в `NoteView`, сейчас 2 сек) — env Vite или общий конфиг приложения

