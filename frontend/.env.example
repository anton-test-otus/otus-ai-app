# URL бэкенд API (по умолчанию в Docker задаётся через docker-compose)
VITE_API_URL=http://localhost:8080/api

# Для Docker задаётся автоматически из корневого APP_AUTH_ENABLED (docker-compose).
# Локально без compose — выставьте вручную:
# VITE_AUTH_ENABLED=true

# Значения по умолчанию (если не заданы в профиле пользователя)
VITE_AUTOSAVE_DELAY_SECONDS=10
VITE_VERSION_CONSOLIDATION_WINDOW_MINUTES=5
